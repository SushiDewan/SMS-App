import 'package:flutter/material.dart';

class CreatePayment {
  String gradeId, subject, feeType, amount;
  CreatePayment(this.gradeId, this.subject, this.feeType, this.amount);
  saveApi(BuildContext context) {}
}
